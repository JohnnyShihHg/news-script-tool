# 新聞文稿整理工具 — 專案規格 v1.1（可開工）

> 給 Claude Code：**從 Phase 1 開始做，做完再往下**。每個 Phase 結束都是一個能用的東西。
> 標 `⚠️ OPEN` 的先問我，其餘不要自行更動。

---

## 0. 分階段交付 ★先讀這節

工具最終要接哪個後端（Google Doc / 其他網頁工具 / 純剪貼簿）**目前未定**。
因此架構原則：**核心轉換引擎與輸出後端徹底解耦**。核心只做「txt → 結構化資料 → 文字」，後端是可抽換的一層。

| Phase | 內容 | 需要外部服務 | 產出 |
|---|---|---|---|
| **1** | txt 解析 → 清洗 → 標點轉換 → 輸出四行格式 → 複製／存檔 | 無，全離線 | **已經可以每天用** |
| **2** | 貼上工作 doc 內容 → slug 比對 → 標示狀態 | 無，全離線 | 自動汰除已處理的 |
| **3** | Gemini 產關鍵字 | Gemini API key | 省去自己想關鍵字（手動觸發） |
| **4** | Google Docs API 直接讀寫 | Google Cloud 專案 | 免手動貼上 |

**Phase 2 刻意用「貼上」而不是 API**：使用者把工作 doc 內容整段複製、貼進工具的文字框，就能做完整比對。這樣 Phase 1+2 完全不需要任何帳號、權限或串接，是一個純本地小工具。Phase 4 只是省掉貼上這個動作，不是必要的。

---

## 1. 技術選型

| 項目 | 選擇 |
|---|---|
| 語言 | Rust 2021 |
| UI | Tauri（前端 HTML/CSS/TS） |
| Regex | `regex` |
| 編碼 | `encoding_rs` |
| 設定 | `toml` + `directories` |
| HTTP（P3 起） | `reqwest` (rustls) |
| OAuth（P4） | `yup-oauth2`，**必須在 Rust 端**（Google 封鎖 embedded webview 登入） |
| 憑證 | `keyring` |

Google Docs / Gemini **都沒有官方 Rust SDK**（Docs 官方支援 Go／Java／JS／.NET／Node／Obj-C／PHP／Python／Ruby），P4 需手打 REST。實際只用到 4 個 endpoint，可控。

核心轉換邏輯全部寫成**純函式**放在獨立 module，不依賴 Tauri、不依賴網路，每個都要有單元測試。

---

## 2. 輸入：iNews txt

```
新聞名稱(標題): 曝韓喊打傅1800     ← slug
樣式: SOT                          ← 清洗判斷欄位（大小寫不一致，出現過 bs）
累積時間: 07:49:58                 ← 播出時間
組: 政                             ← 組別（可能為空）
_______________________________________________________________
[<                                 ← 製作區開始
[ab區]  #a2023最新                 ← 有些檔會有，忽略
[BAR_大] 或 [BAR_最新大]            ← 下一行是標題
T2合成標題範例 記者實地直擊"現場still"
>]                                 ← 製作區結束
這是合成的內文範例第一段…        ← 內文
```

規則：
- `[<` … `>]` 是**成對括號**，中間是播出指令。**內文 = `>]` 之後到檔尾全部**，trim 首尾。
- **標題 = 標題標記下一行、開頭為 `T2` 的內容**，去掉 `T2`。開頭是 `T1`/`T3` → 不抓，列「解析失敗」。
- ⚠️ **OPEN 1 ★標題標記有變體**：三份範例中，SOT 那份用 `[BAR_大]`，另兩份（SO／bs）用 `[BAR_最新大]`。
  - 需確認：**要保留的樣式（SOT 等）會不會也出現 `[BAR_最新大]`？** 若會，兩種都要抓。
  - 還有沒有其他變體（`[BAR_獨家大]`…）？
  - **建議實作**：用 regex `^\[BAR_.*大\]$` 涵蓋所有「大」字結尾的變體，比列舉安全。
  - 附帶觀察：`[BAR_最新大]` 很可能對應工作 doc 標題的 `最新》` 前綴。你說那是手動加的，但 txt 端其實有訊號，之後想自動加的話這裡就是依據。
- 檔內有大量 `[BAR]`／`[BAR_最新]`（分段字卡），**只有帶「大」的才是標題**，不可用 `contains("BAR")`。
- ⚠️ **OPEN 2 ★內文行內標記**：`排雲山莊薪0600`（bs）的內文中夾了一行 `===bs===`，把稿頭與後段隔開。
  - 需確認：**要保留的樣式會不會也有這種 `===xxx===` 行內標記？** 若會，是要移除、還是視為分段？
- 編碼 UTF-8 + CRLF；實作 Big5 fallback；換行統一 `\n`。
- 一個 txt = 一則新聞；txt 端**不含任何人工註記**。
- 表頭以 `key: value` 逐行解析到底線分隔線（`_{10,}`）為止，**全部欄位保留**在資料結構裡。
- **組別可能為空**（`氣象署講雨1100` 的 `組:` 是空的），輸出第一行時要處理，不要留下多餘空格。

---

## 3. 清洗

- **保留白名單**（大小寫無關）：`SOT`、`短sot`、`LIVE`、`海神`、`閃電`、`旋風`、`4G`、`TEL`、`電連`
  - `TEL` 與 `電連` 是同一種東西，**可回寫但要在 UI 標記**。
  - `SOBS` **不納入**。
- **排除黑名單**：`BS`、`SO`（實例中出現過小寫 `bs`，**大小寫無關比對是必要的**）
- **排除**：slug 字串結尾是 `SOU`（大小寫無關）。
- ★ **三態設計，不要二分**：
  - 在白名單 → 保留
  - 在黑名單 → 濾除
  - **兩者都不在 → 標為「未知樣式」，在 UI 單獨列出讓使用者決定**，不要靜默丟棄
  - 理由：樣式清單顯然還沒窮舉完（光三份範例就冒出 SO、SOBS、電連），純白名單會靜默吃掉新樣式，純黑名單會放行垃圾。三態兩邊都不會出錯。
- 缺標題標記或缺 `>]` → 「解析失敗」，**不靜默丟棄**。
- 全部條件寫在 TOML（§7），程式不 hard-code。

---

## 4. 標點正規化 ★最容易寫壞的地方

必須是純函式 + 完整單元測試，測資取自真實稿件。

1. **`"` 轉「」**：標題與內文**都轉**，成對交替（第 1 個→「，第 2 個→」）。單數個 → 轉「並在 UI 提醒。
2. **`.` 轉頓號 `、`**，**但前後皆為數字時保留**：
   - `SUP.海釣.烤肉` → `SUP、海釣、烤肉`
   - `至少111死.逾千棟房受損` → `至少111死、逾千棟房受損`
   - `哥倫比亞7.4強震` → **不動**
3. **半形轉全形**：`, : ; ? ! ( )` → `， ： ； ？ ！ （ ）`。**英文字母、數字維持半形**。
4. **URL 必須排除在所有轉換之外** —— 現有 doc 已經被弄壞的實例：
   ```
   https：//www.youtube.com/watch？v＝PU7gRlGA7wE     ← 死連結
   ```
   實作：先用 regex 圈出 URL 區段，轉換時跳過，最後還原。**這條要有專門的單元測試。**
5. **第一行的播出時間欄位不做轉換**（現有 doc 出現 `08：52：23` 就是被誤轉的結果）。
6. 半形空格保留，僅 trim 行首行尾。
7. `%` `-` `/` `…` 不動。
8. UI 要有**轉換前後 diff 對照**，讓使用者確認沒被弄壞。

---

## 5. 輸出格式

每則四行：

```
{slug} {樣式} {播出時間} {組別}
{標題}
{內文}
{關鍵字，4 個，空格分隔}
```

格式範例（內容為合成示意，非實際稿件）：

```
合成焦點報導1800 SOT 07:49:58 政
合成標題範例 記者實地直擊「現場狀況」
這是合成的內文範例，用來示範四行輸出格式：表頭一行、標題一行、內文一行、關鍵字一行。實際內容會是當日新聞稿的正文。
#合成關鍵字一 #合成關鍵字二 #合成關鍵字三 #合成關鍵字四
```

- 欄位來源：表頭 `新聞名稱(標題)` / `樣式` / `累積時間` / `組`。
- 樣式照原樣輸出（`短sot` 不轉大寫）。
- 關鍵字允許英文數字（`#AI`、`#2026`）。
- 標題前綴**改由工具自動加**（v0.1.3 起，推翻本節原本「人工加、工具不處理」的規定）：
  - 編輯備註含「獨」→ 加 `獨家》`
  - 樣式屬 live/slive/sl/4G/SNG/推播/連線/旋風/閃電/海神 → 加 `最新》`
  - 兩者同時成立只加 `獨家》`；標題已有前綴則不重複加。
- 編輯備註命中發布指示時，**輸出的 slug 行**前面加標記（`【勿上網】`/`【版權問題】`/`【可上網】`）。
  只比對完整詞不比對單字（真實備註有「切勿黑畫面」），且 `slug` 本身不變 —— slug 要拿去和協作文件比對，加了前綴就會全部配不到。
- ✅ 則與則之間空 1 行。

**節次歸屬**：一次匯出的 txt **基本上都是同一節次**，所以不需要用累積時間去算分界（分界每天不固定）。UI 顯示由這批稿件推斷的節次供確認、可手動改，輸出時附上 `======HHMM` 標頭。

---

## 6. 狀態比對（Phase 2）★核心邏輯

**所有前綴一律代表「不上傳」，唯一例外是「抓新」。**

| 類型 | 實例 | 含意 |
|---|---|---|
| 括號類 | `(X)`、`(不上網)`、`(勿上網)`、`(舊帶重開ID)`、`(官方要求下下)`、`(偷拍)`、`(國際)`、`(SOBS)` | 不同人的標記習慣，都是不上傳。`(國際)` 是國際中心用到外國版權素材 |
| 直接類 | `00`、`YH`、`YT-`、`UP`、`KK` | 已有人上傳 |
| | `TEL` | 電連 |
| | `舊帶` | 重複 |

**★ 實作方式：不要去 parse doc 的前綴。**

我們已經有 iNews 來的**乾淨 slug**，所以：

1. 在工作 doc 文字中搜尋包含該 slug 的行
2. 檢查 slug 起始位置**之前有沒有非空白字元**
   - 沒有 → **今天要剪**
   - 有 → 取出前方那段文字；含 `抓新` → **保留（要重貼）**，否則 → **剔除**
3. doc 中查無此 slug → **今天要剪**

好處：**完全不必維護前綴清單**，新同事發明新標記法也不會漏判。設定檔只留 `refresh_keywords = ["抓新"]`。

注意：
- `(勿上網) 合成颱風報導0730` 括號後有空格，前方仍有非空白字元 → 正確剔除。
- 日期區塊頂端的「不上傳清單」（`slug + TAB + 樣式`）也會命中，結論同樣是剔除，不影響正確性。
- 時段不同 = 不同新聞（`阿翰分手了0800` 與 `阿翰分手了0900` 在 doc 中並存），slug 含時段四碼，直接字串相等比對即可，**不需要模糊比對**。
- 比對前兩邊都先正規化全半形與空白。
- ✅ **判定為剔除時只標示、不自動刪除**，由使用者在 UI 最終確認。誤殺一則要剪的新聞代價高。
- ⚠️ **OPEN 5**：doc 裡的 slug 若被人工改過字就搜尋不到。目前假設不會被改。
- ⚠️ **OPEN 6**：部分 slug 沒有時段四碼（`黃文清視訊`），這類怎麼比對？

**工作 doc 的既有結構**（Phase 2 只需要搜尋，不必完整 parse，但 Phase 4 需要）：
```
========0811=========      ← 日期分隔（尾端有 =）
(X)巴士開罐頭0600	短SOT   ← 頂端不上傳清單（slug + TAB + 樣式）
======0700                 ← 時段分隔（尾端無 =）
(國際)哥強震百死0630 短sot 07:00:00 國際中心
（空行）標題（空行）內文（空行）關鍵字
```
真實 doc 很髒：分隔符空格／多空格／TAB 混用、時間半形全形混用、空行數不固定、組別時有時無。解析不出來的段落一律標 `Unparsed` 保留原文，**絕不丟棄**。

---

## 7. 設定檔（TOML）

```toml
[filter]
allowed_styles = ["SOT", "短sot", "LIVE", "海神", "閃電", "旋風", "4G", "TEL", "電連"]
blocked_styles = ["BS", "SO"]        # 其餘未列出的 → 標「未知樣式」交使用者判斷
excluded_slug_suffixes = ["SOU"]
flag_styles = ["TEL", "電連"]        # 可回寫但要標記
title_tag_pattern = '^\\[BAR_.*大\\]$' 

[markers]
# slug 前方有任何非空白字元 = 剔除；除非該段文字含下列關鍵字
refresh_keywords = ["抓新"]

[punctuation]
quotes_to_corner = true              # 標題與內文都轉「」
dot_to_enumeration = true            # . → 、；前後皆數字時保留（7.4）
protect_urls = true                  # 必開
preserve_halfwidth_space = true
[punctuation.map]
"," = "，"
":" = "："
";" = "；"
"?" = "？"
"!" = "！"
"(" = "（"
")" = "）"

[output]
keyword_count = 4
keyword_separator = " "
entry_blank_lines = 1

[gemini]                             # Phase 3
model = "gemini-2.5-flash"

[docs]                               # Phase 4
work_doc_id = ""
archive_folder_id = ""
```

---

## 8. Gemini 關鍵字（Phase 3）

- 每則 4 個，格式 `#OOO`，空格分隔，允許英文數字。
- 可相關，**不可超譯**（不得加入稿件未提及的資訊）。
- Prompt 含：標題＋內文、輸出純 JSON array（**明令禁止 markdown code fence**）、繁體中文、不得杜撰、剛好四個。
- 單則失敗不中斷整批 → 標「產生失敗」，UI 可重試或手動輸入。
- 放在狀態比對**之後**，避免對將剔除的稿子花 API 費用。
- **由使用者按「產生關鍵字」觸發，匯入時不自動跑。** 編輯台約每半小時整理一次，一次動輒數十則，匯入即送會直接撞上免費方案每分鐘請求上限，換來一整排失敗卡片，且可能對使用者還沒決定要不要留的稿子花掉額度。比對仍會自動跑（不花額度，且它正是把待產生清單縮小的那一步）。
- 單次送出上限 `gemini.max_per_run`（預設 15，對應免費方案每分鐘上限；0 = 不限）。超過的部分留到下一次按 —— 已有關鍵字的不會重送，所以再按一次剛好補完剩下的。
- API key 存 OS keyring，不進明碼設定檔。
- 429／5xx 指數退避重試，上限 3 次。

---

## 9. UI（Tauri）

**Phase 1**
1. 匯入：選檔／拖曳資料夾；顯示「載入 N / 通過 N / 濾除 N / 解析失敗 N」，各項可展開查看是哪些檔。
2. 列表：每則顯示 slug、樣式（`TEL`/`電連` 標記）、時間、組別、標題（原始／處理後並陳、可編輯）、內文（可展開編輯）。
3. 節次：顯示推斷值，可手動改。
4. 輸出：`複製全部` 按鈕 + `存成 txt`。
5. 標點 diff 對照面板。

**Phase 2**：新增「貼上工作 doc」文字框；列表加狀態欄（`新` / `抓新(要重貼)` / `已標記` / `解析失敗`）與勾選框。

**Phase 3**：`產生關鍵字` 按鈕、關鍵字可編輯。

**Phase 4**：`讀取工作 doc` / `寫入工作 doc` / `產生保存 doc`。

設定頁直接編輯 §7 的內容，不必手動開檔。

---

## 10. 實作順序

1. iNews txt 解析（純函式 + 測試，真實檔當 fixture）
2. 清洗規則（純函式 + 測試）
3. **標點正規化（純函式 + 測試，URL 保護與 `.` 規則各要專門測試）**
4. 輸出格式化
5. TOML 設定載入
6. Tauri 骨架：匯入 → 列表 → 編輯 → 複製／存檔　← **Phase 1 完成，可以開始每天用**
7. 貼上 doc + slug 比對 + 狀態標示（「抓新」優先序要專門測試）　← Phase 2 完成
8. Gemini 整合　← Phase 3 完成
9. Google OAuth（Rust 端）+ Docs 讀寫　← Phase 4，視需要再做

---

## 11. 待補資料

- [x] SOT 樣式 txt 範例
- [x] 工作 doc 真實內容
- [x] 前綴語意
- [x] BS / SO 樣式的 txt 範例
- [ ] `(OOO抓新)` 的實際範例（Phase 2 需要）
- [ ] Gemini API key（Phase 3 需要）
